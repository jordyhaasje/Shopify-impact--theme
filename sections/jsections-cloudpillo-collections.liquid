{%- comment -%}
  JSections - Cloudpillo Collections
  Version: 1.0.0
  Installed by: Hazify Sections

  Do not modify - updates will overwrite changes.
  To customize, duplicate this section in your theme.
{%- endcomment -%}

{{ 'jsections-cloudpillo-collections.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign section_title = section.settings.heading_prefix | append: ' ' | append: section.settings.heading_emphasis | strip
  assign heading_font = section.settings.heading_font
  assign accent_font = section.settings.accent_font
  assign background_mode = section.settings.background_mode | default: 'inherit'
  assign text_color_mode = section.settings.text_color_mode | default: 'inherit'
  assign color_scheme_class = ''

  if background_mode == 'scheme'
    assign color_scheme_class = ' color-' | append: section.settings.color_scheme | append: ' gradient'
  endif
-%}

{% style %}
  {{ heading_font | font_face: font_display: 'swap' }}
  {{ accent_font | font_face: font_display: 'swap' }}

  #jcp-collections-{{ section.id }} {
    --jcp-heading-font: {{ heading_font.family }}, {{ heading_font.fallback_families }};
    --jcp-heading-style: {{ heading_font.style }};
    --jcp-heading-weight: {{ heading_font.weight }};
    --jcp-accent-font: {{ accent_font.family }}, {{ accent_font.fallback_families }};
    --jcp-accent-style: {{ accent_font.style }};
    --jcp-accent-weight: {{ accent_font.weight }};
  }
{% endstyle %}

<section
  id="jcp-collections-{{ section.id }}"
  class="jcp-collections{{ color_scheme_class }}"
  style="
    --jcp-section-bg: {{ section.settings.section_background }};
    --jcp-text-color: {{ section.settings.text_color }};
    --jcp-card-title-color: {{ section.settings.card_title_color }};
    --jcp-overlay-opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};
    --jcp-overlay-mid-opacity: {{ section.settings.overlay_opacity | divided_by: 200.0 }};
    --jcp-max-width: {{ section.settings.max_width }}px;
    --jcp-card-width: {{ section.settings.card_width }}px;
    --jcp-card-gap: {{ section.settings.card_gap }}px;
    --jcp-card-radius: {{ section.settings.card_radius }}px;
    --jcp-padding-top: {{ section.settings.padding_top }}px;
    --jcp-padding-bottom: {{ section.settings.padding_bottom }}px;
    --jcp-mobile-padding-top: {{ section.settings.mobile_padding_top }}px;
    --jcp-mobile-padding-bottom: {{ section.settings.mobile_padding_bottom }}px;
  "
  data-jcp-section="{{ section.id }}"
  data-jcp-full-width="{{ section.settings.full_width }}"
  data-jcp-background-mode="{{ background_mode }}"
  data-jcp-text-mode="{{ text_color_mode }}"
  data-jcp-animate="{{ section.settings.enable_entrance_animation }}"
  data-jcp-hover-zoom="{{ section.settings.enable_hover_zoom }}"
  {% if section_title != blank %}
    aria-labelledby="jcp-collections-heading-{{ section.id }}"
  {% else %}
    aria-label="Collections"
  {% endif %}
  {{ section.shopify_attributes }}
>
  <div class="jcp-collections__wrapper">
    <div class="jcp-collections__container">
      <div class="jcp-collections__header">
        {%- if section_title != blank -%}
          <h2
            id="jcp-collections-heading-{{ section.id }}"
            class="jcp-collections__heading"
            data-jcp-reveal
          >
            {{ section.settings.heading_prefix | escape }}
            {%- if section.settings.heading_emphasis != blank -%}
              <em>{{ section.settings.heading_emphasis | escape }}</em>
            {%- endif -%}
          </h2>
        {%- endif -%}

        {%- if section.settings.show_arrows and section.blocks.size > 1 -%}
          <div class="jcp-collections__controls" data-jcp-reveal style="--jcp-delay: 200ms;">
            <button
              class="jcp-collections__button"
              type="button"
              data-jcp-prev
              aria-label="{{ section.settings.previous_label | escape }}"
            >
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none" aria-hidden="true" focusable="false">
                <rect x="0.5" y="0.5" width="47" height="47" rx="23.5" stroke="currentColor" stroke-opacity="0.1"></rect>
                <path d="M24.8333 19.6667L20.4999 24L24.8333 28.3333" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"></path>
              </svg>
            </button>
            <button
              class="jcp-collections__button"
              type="button"
              data-jcp-next
              aria-label="{{ section.settings.next_label | escape }}"
            >
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none" aria-hidden="true" focusable="false">
                <rect x="0.5" y="0.5" width="47" height="47" rx="23.5" stroke="currentColor" stroke-opacity="0.1"></rect>
                <path d="M22.1667 28.3333L26.5001 24L22.1667 19.6667" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"></path>
              </svg>
            </button>
          </div>
        {%- endif -%}
      </div>

      <div class="jcp-collections__slider" data-jcp-slider>
        <div class="jcp-collections__track" data-jcp-track role="list">
          {%- for block in section.blocks -%}
            {%- liquid
              assign card_image = block.settings.image
              assign card_title = block.settings.title
              assign card_link = block.settings.link
              assign image_alt = block.settings.image_alt
              assign placeholder_name = block.settings.placeholder | default: 'collection-1'

              if image_alt == blank and card_image != blank and card_image.alt != blank
                assign image_alt = card_image.alt
              endif
            -%}

            <div
              class="jcp-collections__slide"
              role="listitem"
              data-jcp-reveal
              style="--jcp-delay: {{ forloop.index | times: 100 }}ms;"
              {{ block.shopify_attributes }}
            >
              {%- if card_link != blank -%}
                <a class="jcp-collections__card" href="{{ card_link }}" aria-label="{{ card_title | escape }}">
              {%- else -%}
                <div class="jcp-collections__card" role="group" aria-label="{{ card_title | escape }}">
              {%- endif -%}

                {%- if card_title != blank -%}
                  <h3 class="jcp-collections__card-title">{{ card_title | escape }}</h3>
                {%- endif -%}

                {%- if card_image != blank -%}
                  {{
                    card_image
                    | image_url: width: 668
                    | image_tag:
                      loading: 'lazy',
                      widths: '334, 668',
                      sizes: '(min-width: 768px) 334px, calc((100vw - 40px) / 2)',
                      alt: image_alt,
                      class: 'jcp-collections__image'
                  }}
                {%- else -%}
                  {{ placeholder_name | placeholder_svg_tag: 'jcp-collections__placeholder' }}
                {%- endif -%}

              {%- if card_link != blank -%}
                </a>
              {%- else -%}
                </div>
              {%- endif -%}
            </div>
          {%- endfor -%}
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  (() => {
    const sectionId = {{ section.id | json }};
    const selector = `[data-jcp-section="${sectionId}"]`;
    const registryKey = "jcpCollectionInstances";

    window[registryKey] = window[registryKey] || {};

    function cleanup(root) {
      if (!root) return;

      if (root._jcpObserver) {
        root._jcpObserver.disconnect();
        root._jcpObserver = null;
      }

      if (root._jcpHandlers) {
        root._jcpHandlers.forEach(({ element, eventName, handler }) => {
          element.removeEventListener(eventName, handler);
        });
        root._jcpHandlers = [];
      }
    }

    function bind(root, element, eventName, handler) {
      element.addEventListener(eventName, handler);
      root._jcpHandlers = root._jcpHandlers || [];
      root._jcpHandlers.push({ element, eventName, handler });
    }

    function initCarousel(root) {
      const track = root.querySelector("[data-jcp-track]");
      const previousButton = root.querySelector("[data-jcp-prev]");
      const nextButton = root.querySelector("[data-jcp-next]");

      if (!track || !previousButton || !nextButton) return;

      const scrollToSibling = (direction) => {
        const firstSlide = track.querySelector(".jcp-collections__slide");
        const gap = parseFloat(getComputedStyle(track).columnGap || getComputedStyle(track).gap || "0");
        const slideWidth = firstSlide ? firstSlide.getBoundingClientRect().width : 334;
        const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

        track.scrollBy({
          left: direction * (slideWidth + gap),
          behavior: prefersReducedMotion ? "auto" : "smooth",
        });
      };

      bind(root, previousButton, "click", () => scrollToSibling(-1));
      bind(root, nextButton, "click", () => scrollToSibling(1));
    }

    function initReveal(root) {
      const revealItems = root.querySelectorAll("[data-jcp-reveal]");
      if (!revealItems.length) return;

      if (root.dataset.jcpAnimate !== "true" || window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
        revealItems.forEach((item) => item.classList.add("is-visible"));
        return;
      }

      if (typeof IntersectionObserver !== "function") {
        revealItems.forEach((item) => item.classList.add("is-visible"));
        return;
      }

      root._jcpObserver = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (!entry.isIntersecting) return;
            entry.target.classList.add("is-visible");
            root._jcpObserver.unobserve(entry.target);
          });
        },
        { threshold: 0.18 }
      );

      revealItems.forEach((item) => root._jcpObserver.observe(item));
    }

    function init() {
      const root = document.querySelector(selector);
      if (!root) return;

      cleanup(root);
      initCarousel(root);
      initReveal(root);
      window[registryKey][sectionId] = root;
    }

    init();

    document.addEventListener("shopify:section:load", (event) => {
      if (event.detail.sectionId === sectionId) init();
    });

    document.addEventListener("shopify:section:unload", (event) => {
      if (event.detail.sectionId === sectionId) {
        cleanup(window[registryKey][sectionId]);
        delete window[registryKey][sectionId];
      }
    });
  })();
</script>

{% schema %}
{
  "name": "Cloudpillo Collections",
  "tag": "section",
  "class": "section jsections-cloudpillo-collections",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading_prefix",
      "label": "Heading text",
      "default": "Ontdek onze"
    },
    {
      "type": "text",
      "id": "heading_emphasis",
      "label": "Italic heading text",
      "default": "collecties"
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "font_picker",
      "id": "heading_font",
      "label": "Heading and card font",
      "default": "work_sans_n7"
    },
    {
      "type": "font_picker",
      "id": "accent_font",
      "label": "Italic heading font",
      "default": "playfair_display_i4"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "Force full-bleed width",
      "default": true
    },
    {
      "type": "range",
      "id": "max_width",
      "min": 1000,
      "max": 1600,
      "step": 10,
      "unit": "px",
      "label": "Desktop container width",
      "default": 1390
    },
    {
      "type": "range",
      "id": "card_width",
      "min": 280,
      "max": 420,
      "step": 2,
      "unit": "px",
      "label": "Desktop card width",
      "default": 334
    },
    {
      "type": "range",
      "id": "card_gap",
      "min": 0,
      "max": 32,
      "step": 1,
      "unit": "px",
      "label": "Card gap",
      "default": 8
    },
    {
      "type": "range",
      "id": "card_radius",
      "min": 0,
      "max": 32,
      "step": 1,
      "unit": "px",
      "label": "Card corner radius",
      "default": 12
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "label": "Desktop padding top",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "label": "Desktop padding bottom",
      "default": 80
    },
    {
      "type": "range",
      "id": "mobile_padding_top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Mobile padding top",
      "default": 40
    },
    {
      "type": "range",
      "id": "mobile_padding_bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Mobile padding bottom",
      "default": 40
    },
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "select",
      "id": "background_mode",
      "label": "Background mode",
      "options": [
        {
          "value": "inherit",
          "label": "Use theme page background"
        },
        {
          "value": "scheme",
          "label": "Use theme color scheme"
        },
        {
          "value": "custom",
          "label": "Use custom background"
        }
      ],
      "default": "inherit"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Theme color scheme",
      "default": "background-1"
    },
    {
      "type": "color",
      "id": "section_background",
      "label": "Custom background",
      "default": "#f4f4f1"
    },
    {
      "type": "select",
      "id": "text_color_mode",
      "label": "Heading and arrow color mode",
      "options": [
        {
          "value": "inherit",
          "label": "Use theme text color"
        },
        {
          "value": "custom",
          "label": "Use custom color"
        }
      ],
      "default": "inherit"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Custom heading and arrow color",
      "default": "#191d23"
    },
    {
      "type": "color",
      "id": "card_title_color",
      "label": "Card title color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 80,
      "step": 5,
      "unit": "%",
      "label": "Card bottom overlay",
      "default": 45
    },
    {
      "type": "checkbox",
      "id": "show_arrows",
      "label": "Show desktop arrows",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_hover_zoom",
      "label": "Enable image hover zoom",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_entrance_animation",
      "label": "Enable fade-up animation",
      "default": true
    },
    {
      "type": "text",
      "id": "previous_label",
      "label": "Previous arrow label",
      "default": "Vorige collectie"
    },
    {
      "type": "text",
      "id": "next_label",
      "label": "Next arrow label",
      "default": "Volgende collectie"
    }
  ],
  "blocks": [
    {
      "type": "collection_card",
      "name": "Collection card",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Spring deals"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "select",
          "id": "placeholder",
          "label": "Placeholder",
          "options": [
            {
              "value": "collection-1",
              "label": "Collection 1"
            },
            {
              "value": "collection-2",
              "label": "Collection 2"
            },
            {
              "value": "collection-3",
              "label": "Collection 3"
            },
            {
              "value": "collection-4",
              "label": "Collection 4"
            },
            {
              "value": "collection-5",
              "label": "Collection 5"
            },
            {
              "value": "collection-6",
              "label": "Collection 6"
            },
            {
              "value": "image",
              "label": "Image"
            }
          ],
          "default": "collection-1"
        },
        {
          "type": "text",
          "id": "image_alt",
          "label": "Image alt text"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Cloudpillo Collections",
      "blocks": [
        {
          "type": "collection_card",
          "settings": {
            "title": "Spring deals",
            "link": "/nl-nl/collections/bestsellers",
            "placeholder": "collection-1"
          }
        },
        {
          "type": "collection_card",
          "settings": {
            "title": "Toppers",
            "link": "/nl-nl/collections/topmatrassen",
            "placeholder": "collection-2"
          }
        },
        {
          "type": "collection_card",
          "settings": {
            "title": "Hoofdkussens",
            "link": "/nl-nl/collections/hoofdkussens",
            "placeholder": "collection-3"
          }
        },
        {
          "type": "collection_card",
          "settings": {
            "title": "Matrassen",
            "link": "/nl-nl/collections/matrassen",
            "placeholder": "collection-4"
          }
        },
        {
          "type": "collection_card",
          "settings": {
            "title": "Beddengoed",
            "link": "/nl-nl/collections/beddengoed",
            "placeholder": "collection-5"
          }
        },
        {
          "type": "collection_card",
          "settings": {
            "title": "Dekbedden",
            "link": "/nl-nl/collections/dekbedden",
            "placeholder": "collection-6"
          }
        }
      ]
    }
  ]
}
{% endschema %}
