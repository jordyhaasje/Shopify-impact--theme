{%- comment -%}
------------------------------------------------------------------------------------------------------------------------
NOTE TO DEVELOPERS: welcome to Impact theme! We hope that you will enjoy editing this theme as much as we did for
  developing it. We have put a lot of work to make this theme as developer friendly as possible by offering you
  hooks to integrate into critical parts of the theme. You will find the complete technical documentation (including
  all events, dependencies...) in the "documentation.txt" file, located in the Assets folder.
------------------------------------------------------------------------------------------------------------------------
{%- endcomment -%}

<!doctype html>

<html class="no-js" lang="{{ request.locale.iso_code }}" dir="{% render 'direction' %}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, height=device-height, minimum-scale=1.0, maximum-scale=1.0">
    <meta name="theme-color" content="{{ settings.header_background }}">

    <title>{% if page_title == blank %}{{ shop.name }}{% else %}{{ page_title }}{% if current_page != 1 %} &ndash; {{ 'general.page' | t: page: current_page }}{% endif %}{% endif %}</title>

    {%- if page_description -%}
      <meta name="description" content="{{ page_description | escape }}">
    {%- endif -%}

    <link rel="canonical" href="{{ canonical_url }}">

    {%- if settings.favicon -%}
      <link rel="shortcut icon" href="{{ settings.favicon | image_url: width: 96 }}">
      <link rel="apple-touch-icon" href="{{ settings.favicon | image_url: width: 180 }}">
    {%- endif -%}

    {%- comment -%}Few prefetch to increase performance on commonly used third-parties{%- endcomment -%}
    <link rel="preconnect" href="https://cdn.shopify.com">
    <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>
    <link rel="dns-prefetch" href="https://productreviews.shopifycdn.com">

    {%- unless settings.heading_font.system? -%}
      <link rel="preload" href="{{ settings.heading_font | font_url }}" as="font" type="font/woff2" crossorigin>
    {%- endunless -%}

    {%- unless settings.text_font.system? -%}
      <link rel="preload" href="{{ settings.text_font | font_url }}" as="font" type="font/woff2" crossorigin>
    {%- endunless -%}

    {%- render 'social-meta-tags' -%}
    {%- render 'microdata-schema' -%}
    {%- render 'css-variables' -%}
    {%- render 'js-variables' -%}

    <script type="module" src="{{ 'vendor.min.js' | asset_url }}"></script>
    <script type="module" src="{{ 'theme.js' | asset_url }}"></script>
    <script type="module" src="{{ 'sections.js' | asset_url }}"></script>

    {{ content_for_header }}

    {{- 'theme.css' | asset_url | stylesheet_tag: preload: true -}}
  </head>

  <body class="{% if settings.show_page_transition %}page-transition{% endif %} {% if settings.zoom_image_on_hover %}zoom-image--enabled{% endif %}">
    {%- render 'shadow-dom-templates' -%}

    <a href="#main" class="skip-to-content sr-only">{{ 'general.accessibility.skip_to_content' | t }}</a>

    {%- if request.page_type != 'password' -%}
      {%- sections 'header-group' -%}
      {%- sections 'overlay-group' -%}

      {%- if settings.cart_type == 'popover' -%}
        <cart-notification-drawer open-from="bottom" class="quick-buy-drawer drawer"></cart-notification-drawer>
      {%- endif -%}
    {%- endif -%}

    {%- if request.page_type == 'customers/account' or request.page_type == 'customers/order' or request.page_type == 'customers/addresses' -%}
      {%- section 'account-banner' -%}
    {%- endif -%}

    <main role="main" id="main" class="anchor">
      {{ content_for_layout }}

      {%- comment -%}
      IMPLEMENTATION NOTE: due to the very complex logic of margin/padding collapsing in Impact, the footer group is
      added into the main element to ensure that dynamic sections added into the footer group are properly laid out.
      {%- endcomment -%}
      {%- if request.page_type != 'password' -%}
        {%- sections 'footer-group' -%}
      {%- endif -%}
    </main>
    <!-- BLACK FRIDAY POPUP - soft pink fashion modal -->
    <style>
      .bf-fashion-popup {
        position: fixed;
        inset: 0;
        z-index: 9999;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 18px;
        background: rgba(20, 14, 17, .44);
        backdrop-filter: blur(8px);
        opacity: 0;
        visibility: hidden;
        pointer-events: none;
        transition: opacity .45s ease, visibility .45s ease;
      }

      .bf-fashion-popup.is-visible {
        opacity: 1;
        visibility: visible;
        pointer-events: auto;
      }

      .bf-fashion-popup__card {
        position: relative;
        width: min(100%, 440px);
        overflow: hidden;
        border-radius: 28px;
        padding: 30px 24px 24px;
        text-align: center;
        color: #22161a;
        background:
          radial-gradient(circle at 16% 12%, rgba(255, 203, 221, .8), transparent 34%),
          linear-gradient(180deg, #ffffff 0%, #fff1f6 100%);
        box-shadow: 0 28px 90px rgba(50, 18, 30, .26), 0 0 0 1px rgba(255, 255, 255, .8) inset;
        transform: translateY(26px) scale(.96);
        transition: transform .45s cubic-bezier(.2,.8,.2,1);
      }

      .bf-fashion-popup.is-visible .bf-fashion-popup__card {
        transform: translateY(0) scale(1);
      }

      .bf-fashion-popup__close {
        position: absolute;
        top: 14px;
        right: 16px;
        width: 34px;
        height: 34px;
        border: 0;
        border-radius: 999px;
        background: rgba(255, 255, 255, .7);
        color: #8a6670;
        cursor: pointer;
        font-size: 22px;
        line-height: 1;
      }

      .bf-fashion-popup__eyebrow {
        display: inline-flex;
        margin-bottom: 12px;
        padding: 7px 12px;
        border-radius: 999px;
        background: #ffe0eb;
        color: #d8477b;
        font-size: 11px;
        font-weight: 800;
        letter-spacing: .16em;
        text-transform: uppercase;
      }

      .bf-fashion-popup__title {
        margin: 0;
        color: #1d1518;
        font-family: var(--heading-font-family, inherit);
        font-size: clamp(30px, 7vw, 42px);
        line-height: .98;
        letter-spacing: -.045em;
      }

      .bf-fashion-popup__text {
        max-width: 330px;
        margin: 14px auto 0;
        color: #7b636a;
        font-size: 15px;
        line-height: 1.6;
      }

      .bf-fashion-popup__discount {
        margin: 18px auto 0;
        color: #ff4f8f;
        font-size: clamp(34px, 9vw, 52px);
        font-weight: 900;
        line-height: .95;
        letter-spacing: -.06em;
      }

      .bf-fashion-popup__button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        margin-top: 24px;
        min-height: 48px;
        padding: 0 28px;
        border-radius: 999px;
        color: #fff;
        background: linear-gradient(135deg, #ff8fbd, #ff4f8f);
        box-shadow: 0 14px 34px rgba(255, 79, 143, .35);
        font-size: 14px;
        font-weight: 800;
        text-decoration: none;
        transition: transform .22s ease, box-shadow .22s ease;
      }

      .bf-fashion-popup__button:hover {
        transform: translateY(-2px);
        box-shadow: 0 18px 44px rgba(255, 79, 143, .45);
      }

      .bf-fashion-popup__note {
        margin-top: 14px;
        color: #9b7f87;
        font-size: 12px;
      }

      @media screen and (max-width: 480px) {
        .bf-fashion-popup { align-items: flex-end; padding: 12px; }
        .bf-fashion-popup__card { border-radius: 24px; padding: 28px 18px 22px; }
      }
    </style>

    <div class="bf-fashion-popup" id="bfFashionPopup" aria-hidden="true">
      <div class="bf-fashion-popup__card" role="dialog" aria-modal="true" aria-label="Black Friday Sale">
        <button class="bf-fashion-popup__close" type="button" data-bf-close aria-label="Sluit popup">×</button>
        <div class="bf-fashion-popup__eyebrow">Black Friday</div>
        <h2 class="bf-fashion-popup__title">Soft pink styles, limited sale</h2>
        <p class="bf-fashion-popup__text">Shop je favoriete kledingstukken tijdelijk met extra Black Friday voordeel.</p>
        <div class="bf-fashion-popup__discount">Tot 50% korting</div>
        <a class="bf-fashion-popup__button" href="/collections/all">Shop de sale</a>
        <div class="bf-fashion-popup__note">Alleen tijdelijk beschikbaar</div>
      </div>
    </div>

    <script>
      (function() {
        var popup = document.getElementById('bfFashionPopup');
        if (!popup) return;

        var closeButton = popup.querySelector('[data-bf-close]');
        var storageKey = 'bf_fashion_popup_seen_v1';

        function showPopup() {
          popup.classList.add('is-visible');
          popup.setAttribute('aria-hidden', 'false');
        }

        function closePopup() {
          popup.classList.remove('is-visible');
          popup.setAttribute('aria-hidden', 'true');
          try { window.localStorage.setItem(storageKey, '1'); } catch (error) {}
        }

        try {
          if (!window.localStorage.getItem(storageKey)) {
            window.setTimeout(showPopup, 2500);
          }
        } catch (error) {
          window.setTimeout(showPopup, 2500);
        }

        if (closeButton) closeButton.addEventListener('click', closePopup);
        popup.addEventListener('click', function(event) {
          if (event.target === popup) closePopup();
        });
        document.addEventListener('keydown', function(event) {
          if (event.key === 'Escape' && popup.classList.contains('is-visible')) closePopup();
        });
      })();
    </script>
  </body>
</html>