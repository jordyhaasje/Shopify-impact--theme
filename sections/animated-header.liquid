{% schema %}
{
  "name": "Animated header",
  "settings": [
    { "type": "range", "id": "card_offset_right", "label": "Desktop card right offset", "min": 0, "max": 200, "step": 2, "unit": "px", "default": 20 }
  ],
  "presets": [{"name":"Animated header"}]
}
{% endschema %}